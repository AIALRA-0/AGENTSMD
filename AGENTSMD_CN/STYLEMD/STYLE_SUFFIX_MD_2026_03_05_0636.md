# STYLE_SUFFIX_MD_2026_03_05_0636

## Metadata

* **修改者：** Codex
* **Type：** SUFFIX
* **Key：** MD

## Summary

* 固化 Markdown 文档风格规则，统一标题层级、列表语义与代码块标注，提升可读性与可维护性。

## Style Details

* **格式规则：** 标题从 `#` 顺序递进、段落之间保留一个空行、列表项每行表达一个完整语义、代码块必须带语言标识。
* **注释规范：** 正式文档禁止遗留 HTML 隐藏注释，临时备注必须在提交前删除或转为明确条目。
* **命名规则：** 文档文件名统一使用全大写目录前缀 + 语义 Key + 时间戳，目录内同主题按 Key 聚合。
* **其他规则：** 表格列名使用中文业务语义，避免混用中英列名导致索引理解偏差。

## Thought

* AGENTSMD 体系主要载体是 Markdown，文档风格不统一会直接影响检索效率与执行准确性。
* 标题层级与代码块语言标注是自动检查和快速阅读的关键基础约束。
* 减少注释噪声和命名歧义可以降低后续模板校验脚本的复杂度。

## Action

* 按 STYLE 模板新增 `Key=MD` 的后缀风格条目并固化核心规则。
* 将格式、注释、命名拆分为独立单行规则，便于脚本解析与人工审查。
* 同步更新 `STYLE_INDEX.md`，将 `MD` 条目纳入可检索索引。

## Observation

* 该条目可直接用于约束后续所有 AGENTSMD 文档编辑行为。
* 规则内容与当前已落地的 CHANGEMD/DECISIONMD/ENVIRONMENTMD 写法保持一致。
* 后续若规则变更，只需更新 `MD` 同 Key 条目并刷新时间戳即可。
