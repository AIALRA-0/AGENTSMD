# API_TEMPLATE

## 记录规则

* APIMD 为条目模式（entry），按 API 主题 `Key` 维护统一接口规范。
* 同一 `Key` 仅保留一个当前有效条目。
* `Key` 已存在时更新条目并刷新文件名时间戳；`Key` 不存在时新建条目。

## Metadata

* **修改者：** [执行器Agent的名称]
* **Type：** [INTERNAL / EXTERNAL]
* **Key：** [API 主题唯一键]

## Summary

* [总结本 API 条目的核心内容，必须一句话，单行输出]

## Source

* [SRC-001 | 类型(WEB/PDF/REPORT/REPO/API/LOCAL_FILE/LOG/INTERVIEW/OTHER) | 路径或URL | 证据用途]
* [SRC-002 | 类型(WEB/PDF/REPORT/REPO/API/LOCAL_FILE/LOG/INTERVIEW/OTHER) | 路径或URL | 证据用途]
* [其他必须记录的来源证据，可以多条输出，每条一句话]

## Endpoint

* [API-001 | 方法(GET/POST/PUT/DELETE/...) | 路径或URL | 作用说明 | 请求体/参数摘要 | 成功响应摘要]
* [API-002 | 方法(GET/POST/PUT/DELETE/...) | 路径或URL | 作用说明 | 请求体/参数摘要 | 成功响应摘要]
* [其他必须记录的接口端点细节，可以多条输出，每条一句话]

## Usage

* [调用场景与前置条件，可以多条输出，每条一句话]
* [最小可用调用示例（命令或SDK），可以多条输出，每条一句话]
* [错误处理与重试策略，可以多条输出，每条一句话]

## Token Policy

* [鉴权方式（如 Bearer/API-Key/OAuth2），可以多条输出，每条一句话]
* [token 来源位置（如环境变量/密钥管理系统），可以多条输出，每条一句话]
* [token 轮换、过期与权限边界规则，可以多条输出，每条一句话]
* [禁止写入真实明文 token，若需示例仅使用脱敏占位符]

## Quota & Maintenance

* [用量口径（请求量、token、并发、带宽等），可以多条输出，每条一句话]
* [限流/配额阈值与超限策略，可以多条输出，每条一句话]
* [维护责任人或维护流程（升级、回滚、告警），可以多条输出，每条一句话]

## Thought

* [本条 API 规则制定的背景原因，可以多条输出，每条一句话]
* [关键约束与风险边界判断，可以多条输出，每条一句话]
* [其他必须记录的接口思考，可以多条输出，每条一句话]

## Action

* [本次条目落地动作，可以多条输出，每条一句话]
* [本次联动更新动作（如 SPEC/TEST/TOOL），可以多条输出，每条一句话]
* [其他必须记录的执行细节，可以多条输出，每条一句话]

## Observation

* [接入验证与兼容性结果，可以多条输出，每条一句话]
* [对开发、测试、维护的影响，可以多条输出，每条一句话]
* [其他必须记录的观察细节，可以多条输出，每条一句话]
